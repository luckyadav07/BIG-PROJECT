services:
  redis:
    image: redis
    ports:
      - "6379:6379"

  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env  
    depends_on:
      - redis
      - mongodb

    
    